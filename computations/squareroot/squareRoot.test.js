const squareRoot = require('./squareRoot');

describe("check if the same number is multiplied gives a square root of the number", () => {
    test("calculating square root of 25 should be 5", () => {
        expect(Math.sqrt(25)).toBe(5);

    });

});